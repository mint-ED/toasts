setup

```shell
npm init --yes
add to package.json:  "type": "module"
npm install @alch/alchemy-web3
npm install node-fetch
npm install axios
npm install dotenv --save

to each js script file, add: import 'dotenv/config';

```

run

```shell
node folder/file.js

```